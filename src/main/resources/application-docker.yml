spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/taskflow
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  data:
    redis:
      host: redis
      port: 6379

  mail:
    host: mailhog
    port: 1025

app:
  jwt:
    secret: ${JWT_SECRET}